program Arrays;
   
procedure main()
var i : int;
    j : int;
    k : int;
    a : array of array of int;
begin
    // fill array
    a := new(2,3);

    i := 0;
    j := 0;
    write( a[4,5]);
    while j < 3 do
    begin
      write( a[0,j]);
      j := j + 1;
      if j < 3 then write(',');
    end;

    i := 1;
    j := 0;
    while j < 3 do
    begin
      write( a[0,j]);
      j := j + 1;
      if j < 3 then write(',');
    end;

end; // main
